---
- name: Install Chocolatey Packages
  win_chocolatey:
    name: "{{ item }}"
    state: latest
  loop: "{{ [
    'chocolatey',
    'everything' if install_everything else [],
  ] | flatten(levels=1) }}"
